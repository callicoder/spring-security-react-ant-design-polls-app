import { createSelector, createFeatureSelector } from '@ngrx/store';
// import { UserInfo, UserState, USER_STATE_NAME } from './user.state';

// const selectUserState = createFeatureSelector<UserInfo>(USER_STATE_NAME);

// const getCurrentUser = createSelector(
//   selectUserState,
//   (user: UserInfo) => user
// );

// export const UserStoreQuery = {
//   getCurrentUser,
// };
